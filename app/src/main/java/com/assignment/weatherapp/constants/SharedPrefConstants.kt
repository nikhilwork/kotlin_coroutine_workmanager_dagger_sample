package com.assignment.weatherapp.constants

class SharedPrefConstants {
    companion object{
        const val WEATHER_APP_PREF = "weather_app_pref"
        const val KEY_LAST_WEATHER_RESULT = "key_last_weather_result"
        const val KEY_LAST_LOCATION = "key_last_location"
    }
}